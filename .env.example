# Sanity (required for design system and Studio)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Optional: fallback site hostname when request hostname does not match any site
# NEXT_PUBLIC_DEFAULT_SITE_HOSTNAME=localhost
