# Baseline (UI-only) optional values
NEXT_PUBLIC_SITE_LOGO_URL=

# Optional: fallback hostname used by the baseline layout metadata
NEXT_PUBLIC_DEFAULT_SITE_HOSTNAME=localhost

# Optional Sanity integration (only needed when CMS path is enabled)
# NEXT_PUBLIC_SANITY_PROJECT_ID=
# NEXT_PUBLIC_SANITY_DATASET=
# NEXT_PUBLIC_SANITY_API_VERSION=
# ALLOWED_SITE_HOSTNAMES=
